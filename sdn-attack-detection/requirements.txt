# ============================================================================
# Πλήρες requirements — καλύπτει ML pipeline, live app (Flask/Dash) και tests.
#   pip install -r requirements.txt
#   bash run_all.sh
#   pytest tests/ -v
# ============================================================================

# --- ML pipeline (Μέθοδος Β) ---
# ΣΗΜΕΙΩΣΗ: τα αποθηκευμένα models/*.pkl εκπαιδεύτηκαν με scikit-learn 1.9
# (βλ. models/meta.json -> trained_with). Φόρτωσή τους με παλαιότερη έκδοση
# βγάζει InconsistentVersionWarning — γι' αυτό ζητάμε >=1.9. Εναλλακτικά,
# αναδημιούργησέ τα με τη δική σου έκδοση: bash run_all.sh
scikit-learn>=1.9
pandas>=2.0
numpy>=1.24
matplotlib>=3.7
seaborn>=0.12
joblib>=1.3

# --- Ερμηνευσιμότητα (shap_analysis.py) ---
shap>=0.44

# --- Extended ML (extended_eval.py — με sklearn fallbacks αν λείπουν) ---
xgboost>=1.7
lightgbm>=3.3

# --- Live εφαρμογή: controller + dashboard (Μέθοδος Α, app_sdn/) ---
flask>=2.3
dash>=2.14
plotly>=5.18
requests>=2.31

# --- Tests ---
pytest>=7.4

# ============================================================================
# ΠΡΟΑΙΡΕΤΙΚΑ (δεν εγκαθίστανται αυτόματα):
#
# Deep Learning (deep_learning.py — MLP/1D-CNN):
#   pip install tensorflow>=2.15
#   Αν λείπει, το run_all.sh παραλείπει το βήμα Deep Learning με μήνυμα.
#
# SDN controller (εναλλακτική υλοποίηση controller/ με Ryu/POX):
#   pip install ryu           # ή το συντηρούμενο fork: pip install os-ken
#   (με os-ken άλλαξε τα imports 'ryu.' -> 'os_ken.')
#
# Mininet (live προσομοίωση, μόνο Linux — ΟΧΙ pip):
#   sudo apt update && sudo apt install mininet
#   sudo apt install hping3 nmap iperf   # εργαλεία επιθέσεων στο lab VM
# ============================================================================
